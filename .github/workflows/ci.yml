name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: payments
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Load database schema
        run: PGPASSWORD=postgres psql -h localhost -U postgres -d payments -f app/schema.sql

      - name: Start the API
        run: |
          DATABASE_URL=postgresql://postgres:postgres@localhost:5432/payments \
          uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          sleep 3

      - name: Run race condition test
        run: python race_test.py --n 100
